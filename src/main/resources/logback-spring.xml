<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 루트 로그 출력 레벨 -->
    <property name="LOG_LEVEL" value="info"/>
    <!-- 로그 파일이 저장될 경로-->
    <property name="LOG_PATH" value="./logs"/>
    <!-- 로그 파일 이름 -->
    <property name="LOG_FILE_NAME" value="user2"/>
    <!-- 로그 출력 패턴 -->
    <property name="LOG_PATTERN" value="%d{yy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{0}:%line] - %msg%n"/>
    <property name="CONSOLE_LOG_PATTERN" value="[%yellow(%d{yyyy-MM-dd HH:mm:ss.SSS})] [%magenta(%thread)] : %highlight(%-5level) [%boldCyan(%logger{20}):%green(%line)] - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 파일 이름 패턴 수정 -->
            <fileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}.${LOG_FILE_NAME}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 로그 전역 세팅 -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>